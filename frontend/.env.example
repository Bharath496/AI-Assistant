# Optional browser API base URL override.
# Leave unset for Cloudflare Pages. The web app will use same-origin /api.
# For local dev or a remote backend, set the full URL ending in /api.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
